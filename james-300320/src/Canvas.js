import React from "react";
import styled from "@emotion/styled";
const HalfDivider = styled.div`
  width: 50%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const Canvas = ({ colour }) => {
  return <HalfDivider></HalfDivider>;
};

export default Canvas;
